interface LoginModalProps {
  openLogin: boolean;
  onCloseLogin: React.MouseEventHandler<HTMLButtonElement>;
}

interface SignupModalProps {
  openSignup: boolean;
  onCloseSignup: React.MouseEventHandler<HTMLButtonElement>;
}
